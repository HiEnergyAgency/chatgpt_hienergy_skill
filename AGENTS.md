Always use the configured `hienergy` MCP server for HiEnergy advertiser, deal, contact, transaction, and broader API-surface questions before falling back to the local FastMCP scaffold in this repo.

Use the local Python server code only when the task is to develop, debug, or extend the MCP implementation itself.
